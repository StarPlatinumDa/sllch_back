<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vueadmin.system.mapper.GroupVerificationMapper">
    <insert id="addVerification" parameterType="GroupVerification">
        insert into group_verification(`user_id`, `from_id`, `to_id`, `verification_status`, `verification_time`, `group_id`)
        values (#{userId}, #{fromId}, #{toId}, #{verificationStatus}, #{verificationTime}, #{groupId})
    </insert>

    <delete id="delVerification" parameterType="int">
        delete from group_verification
        where user_id=#{userId}
    </delete>

    <select id="selectAllByUserId" parameterType="int" resultType="GroupVerification">
        select * from group_verification
        where user_id=#{userId} order by verification_time desc
    </select>

    <update id="updateVerificationOfInvite" parameterType="Map">
        update group_verification
        <set>
            <if test="verificationStatus != null">
                verification_status=#{verificationStatus},
            </if>
            <if test="verificationTime != null">
                verification_time=#{verificationTime}
            </if>
        </set>
        where from_id=#{fromId} and to_id=#{toId} and group_id=#{groupId} and verification_status=0
    </update>

    <update id="updateVerificationOfApply" parameterType="Map">
        update group_verification
        <set>
            <if test="verificationStatus != null">
                verification_status=#{verificationStatus},
            </if>
        </set>
        where from_id=#{fromId} and group_id=#{groupId} and verification_status=0
    </update>

    <select id="selectGroupVerifications" parameterType="GroupVerification" resultType="GroupVerification">
        select * from group_verification
        where user_id=#{userId} and from_id=#{fromId} and group_id=#{groupId} and verification_status=0
    </select>
</mapper>